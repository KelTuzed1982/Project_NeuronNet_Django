  Проект содержит питоновские модули, при помощи которых можно создать параметрезированную нейронную сеть и сохранять её в БД при помощи джанги. Модули включают в себя как необходимве для работы джанги модули, так и непосредственно модули для создания сети. Также присутвуют файл БД SQLite (в корне файл DataBaseSQLite.db), обучающая выборка (в папке main файл learn.txt), питоновский модуль для генерации обучающей выборки синуса (в папке main файл gen_sin.py).
  
  Соотвественно для создания и работы нейронной сети нужен Python версии 2.7.2, Django версии 1.3.1 (скорее всего подойдут и другие версии джанги и питона, но тестилось только на этих). Еще в переменные среды добавьте DJANGO_SETTINGS_MODULE со значением settings (ой, ваще убого сделано - может быть потом исправлю).
  
  Чтобы создать нейронную сеть:  
  1)скачайте все файлы из репозитория  
  2)откройте файл NeuronNet и подредактируйте 6ю строчку в которой задается pythonpath для проекта на ваше расположение проекта (ой, согласен, как это убого сделано, но может в будущих версиях исправим)  
  3)импортируйте из модуля NeuronNet_djang (находится в папке main) класс NeuronNet.   
  4) Создайте экземпляр класса NeuronNet (без указания каких-либо параметров для конструктора). Чтобы посмотреть нейронную сеть, вызовете метод show у экземпляра класса(также без указания каких-либо параметров).   

  В принципе есть пример использования нейронной сети. Посмотрите файл test.py (в папке main).
  
  Остальные подробности по использованию сети ищите в комментариях к коду. :/
  